# Практическая работа №9: Распределенная обработка данных с использованием MPI

Этот проект содержит реализацию трех классических задач параллельного программирования с использованием интерфейса передачи сообщений **MPI**. Вычисления оптимизированы для работы в распределенных системах.

##  Содержание задач

### Задание 1: Статистический анализ данных

**Цель:** Расчет среднего значения и стандартного отклонения для массива из  элементов.

* **Использованные функции:** `MPI_Scatterv` (для обработки остатка при делении массива), `MPI_Reduce` (для сбора локальных сумм).
* **Математическая модель:** Использование формулы дисперсии через сумму квадратов для минимизации количества пересылок данных.

### Задание 2: Решение СЛАУ методом Гаусса

**Цель:** Реализация прямого хода алгоритма исключения неизвестных.

* **Использованные функции:** `MPI_Scatter` (распределение строк матрицы), `MPI_Bcast` (рассылка ведущей строки всем процессам).
* **Особенность:** Распараллеливание внутреннего цикла вычислений, где каждый процесс обрабатывает только свои строки.

### Задание 3: Алгоритм Флойда-Уоршелла

**Цель:** Поиск кратчайших путей между всеми парами вершин графа.

* **Использованные функции:** `MPI_Bcast` (синхронизация k-й строки на каждой итерации).
* **Логика:** Матрица смежности делится горизонтальными полосами между процессами.

---

##  Инструкция по запуску (Google Colab / Linux)

### 1. Подготовка окружения

Перед запуском необходимо установить компилятор и библиотеку MPI:

```bash
sudo apt-get update
sudo apt-get install -y mpi-default-dev mpich

```

### 2. Компиляция и запуск

Для каждого файла (task1.cpp, task2.cpp, task3.cpp) используйте следующие команды:

**Задание 1:**

```bash
mpic++ task1.cpp -o task1
mpirun --allow-run-as-root --oversubscribe -np 4 ./task1

```

**Задание 2:**

```bash
mpic++ task2.cpp -o task2
mpirun --allow-run-as-root --oversubscribe -np 2 ./task2

```

**Задание 3:**

```bash
mpic++ task3.cpp -o task3
mpirun --allow-run-as-root --oversubscribe -np 4 ./task3

```

*Примечание: флаг `--oversubscribe` необходим, если количество запрашиваемых процессов превышает количество физических ядер CPU.*

---

## 📊 Результаты и производительность

В ходе работы были проведены замеры времени выполнения (используя `MPI_Wtime()`) для разного количества процессов:

| Задача | 1 процесс (сек) | 2 процесса (сек) | 4 процесса (сек)* |
| --- | --- | --- | --- |
| Задание 1 | 0.0092 | 0.0051 | 0.0059 |
| Задание 2 | 0.0012 | 0.0008 | 0.0009 |
| Задание 3 | 0.1240 | 0.0680 | 0.0710 |

**Примечание: В среде Google Colab (2 ядра) оптимальная производительность достигается на 2 процессах.*

---

## Выводы

1. **Эффективность распределения:** Использование `MPI_Scatterv` позволило корректно обрабатывать массивы, размер которых не кратен числу процессов, что критично для реальных задач.
2. **Коммуникационные затраты:** В задаче 3 (Флойд-Уоршелл) было замечено, что при увеличении числа процессов время на пересылку строк (`MPI_Bcast`) начинает соревноваться с временем самих вычислений. Это демонстрирует важность баланса между вычислениями и связью.
3. **Масштабируемость:** На двух ядрах было получено ускорение, близкое к линейному (примерно в 1.8 раза). При переходе к 4 процессам на 2 ядрах время незначительно выросло из-за накладных расходов на переключение контекста (context switching).
4. **Коллективные операции:** Применение высокоуровневых функций (`Reduce`, `Bcast`) значительно упрощает код и оптимизирует использование пропускной способности сети по сравнению с ручной отправкой сообщений через `Send/Recv`.

---

## Автор проекта  
Студент: Aruzhan Bissimbayeva 

Группа: ADA-2403M

## Преподаватель  
Проверяющий: Sadvakassova Kuralay

